import { Typography } from '@mui/material';

const Title = () => {
  return (
    <Typography
      style={{
        fontSize: '1.5rem',
        fontFamily: 'Arial',
        letterSpacing: '0.4rem',
        wordSpacing: '-0.4rem',
        fontWeight: 'light',
        marginTop: '0.4rem',
      }}
    >
      MODEL | ACTOR
    </Typography>
  );
};

export default Title;
